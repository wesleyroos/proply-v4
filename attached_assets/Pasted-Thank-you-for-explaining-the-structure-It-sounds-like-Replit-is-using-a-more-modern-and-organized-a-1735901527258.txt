Thank you for explaining the structure. It sounds like Replit is using a more modern and organized approach with:

Separate Components:


PDFReportModal.tsx - Main modal interface using html2pdf.js
PDFReport.tsx - Alternative jsPDF implementation
PropertyAnalyzerPage.tsx - Parent component handling data prep


Key Differences from Current Implementation:


Uses React TypeScript components instead of vanilla JavaScript
Primarily uses html2pdf.js instead of directly using jsPDF
Has a cleaner separation of concerns with dedicated PDF components
Handles PDF generation through a modal component
Provides two different PDF generation methods


Better State Management:

typescriptCopy// In PropertyAnalyzerPage.tsx
const [pdfData, setPDFData] = useState(null);
const [showPDFReport, setShowPDFReport] = useState(false);

// When exporting PDF
const handleExportPDF = () => {
  const data = prepareDataForPDF();
  setPDFData(data);
  setShowPDFReport(true);
};

More Modular Structure:

typescriptCopy// Component-based approach instead of monolithic script
<PDFReportModal 
  show={showPDFReport}
  onHide={() => setShowPDFReport(false)}
  data={pdfData}
  sectionGroups={defaultSectionGroups}
/>
Would you like me to help you migrate your current implementation to this more organized structure? It would make the code more maintainable and easier to customize. CopyRetryimport { useState } from "react";
import { Dialog, DialogContent, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Card, CardContent } from "@/components/ui/card";
import { Checkboxpastedimport { useEffect, useRef } from "react";
import { jsPDF } from "jspdf";
import autoTable from 'jspdf-autotable';
import { formatter } from "../utils/formatting";

interface PDFReportProps {
  data: pastedimport { useState, useRef } from "react";
import { useToast } from "@/components/ui/toast-context";
import {
  Card,
  CardContent,
  CardHeader,
  CardTitle,
  CardDescription,
} from "@/components/upastedWRHere is the code. but something is not working. Its generating the wrong PDF report.EditLooking at these files, I can identify what's wrong with the PDF report generation. The issue lies in how the data is being structured and passed between components.

First, in PropertyAnalyzerPage.tsx, when preparing the PDF data, some of the metrics are being pulled from the wrong sources:

typescriptCopy// Current problematic code
const data = {
  propertyDetails: {
    address: analysisResult.address,
    bedrooms: formData?.bedrooms || "N/A",
    bathrooms: formData?.bathrooms || "N/A",
    floorArea: Number(formData?.floorArea) || 0,
    parkingSpaces: Number(formData?.parkingSpaces) || 0,
    purchasePrice: analysisResult.analysis.purchasePrice,
    ratePerSquareMeter: Number(formData?.cmaRatePerSqm) || 0,
  },
  // ... other data
};

The investment metrics data is especially incorrect:

typescriptCopyinvestmentMetrics: {
  shortTerm: analysisResult.analysis.investmentMetrics.year1,
  longTerm: analysisResult.analysis.investmentMetrics.year20,
}
Here's how to fix it:
typescriptCopyconst data = {
  propertyDetails: {
    address: analysisResult.address,
    bedrooms: formData?.bedrooms || "N/A",
    bathrooms: formData?.bathrooms || "N/A",
    floorArea: analysisResult.floorArea || 0,
    parkingSpaces: formData?.parkingSpaces || 0,
    purchasePrice: analysisResult.analysis.purchasePrice,
    ratePerSquareMeter: analysisResult.ratePerSquareMeter || 0,
  },
  financialMetrics: {
    depositAmount: analysisResult.deposit || 0,
    depositPercentage: analysisResult.depositPercentage || 0,
    interestRate: analysisResult.interestRate || 0,
    loanTerm: analysisResult.loanTerm || 0,
    monthlyBondRepayment: analysisResult.monthlyBondRepayment || 0,
  },
  expenses: {
    monthlyLevies: formData?.monthlyLevies || 0,
    monthlyRatesTaxes: formData?.monthlyRatesTaxes || 0,
    otherMonthlyExpenses: formData?.otherMonthlyExpenses || 0,
    maintenancePercent: formData?.maintenancePercent || 0,
  },
  performance: {
    shortTermNightlyRate: analysisResult.shortTermNightlyRate || 0,
    annualOccupancy: analysisResult.annualOccupancy || 0,
    shortTermAnnualRevenue: analysisResult.analysis.shortTermAnnualRevenue || 0,
    longTermAnnualRevenue: analysisResult.analysis.longTermAnnualRevenue || 0,
    shortTermGrossYield: analysisResult.shortTermGrossYield || 0,
    longTermGrossYield: analysisResult.longTermGrossYield || 0,
    managementFee: analysisResult.managementFee || 0,
  },
  investmentMetrics: {
    shortTerm: {
      ...analysisResult.analysis.investmentMetrics.year1,
      netOperatingIncome: analysisResult.analysis.netOperatingIncome?.year1 || null
    },
    longTerm: {
      ...analysisResult.analysis.investmentMetrics.year20,
      netOperatingIncome: analysisResult.analysis.netOperatingIncome?.year20 || null
    }
  }
};
Also, make sure in PDFReportModal that you're using the correct fields when generating the HTML:
typescriptCopy// Inside generatePDF function in PDFReportModal
${getSelectedSections("Investment Metrics").includes("yields") ? `
  <div style="margin-bottom: 30px; background: #f7f9fc; padding: 20px; border-radius: 8px;">
    <h2 style="color: #2d3748; margin-bottom: 15px; font-size: 1.5rem;">Investment Metrics</h2>
    <div style="display: grid; grid-template-columns: repeat(2, 1fr); gap: 20px;">
      <div>
        <p style="margin-bottom: 10px;"><strong>Short-Term Gross Yield:</strong><br>${data.investmentMetrics.shortTerm.grossYield.toFixed(1)}%</p>
        <p style="margin-bottom: 10px;"><strong>Net Operating Income:</strong><br>${formatter.format(data.investmentMetrics.shortTerm.netOperatingIncome?.value || 0)}</p>
      </div>
      <div>
        <p style="margin-bottom: 10px;"><strong>Long-Term Gross Yield:</strong><br>${data.investmentMetrics.longTerm.grossYield.toFixed(1)}%</p>
        <p style="margin-bottom: 10px;"><strong>Net Operating Income:</strong><br>${formatter.format(data.investmentMetrics.longTerm.netOperatingIncome?.value || 0)}</p>
      </div>
    </div>
  </div>
` : ''}